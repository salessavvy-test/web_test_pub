const addThreeNumbers = require('./addThreeNumbers');

// Example usage
const num1 = 5;
const num2 = 10;
const num3 = 15;

const result = addThreeNumbers(num1, num2, num3);
console.log(`${num1} + ${num2} + ${num3} = ${result}`);
